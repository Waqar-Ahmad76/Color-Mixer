# Color-Mixer
A simple React application built to practice state management and event handling (onChange, onClick). Users can adjust Red, Green, and Blue sliders in real-time to mix and display a custom color.
